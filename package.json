{
  "name": "blastr",
  "version": "0.1.0",
  "private": true,
  "description": "The Blastr Frontend Compiler Framework",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "format": "prettier --write ."
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  },
  "keywords": [],
  "author": "CorentynDevPro",
  "license": "ISC",
  "packageManager": "pnpm@10.10.0"
}
